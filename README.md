# Submission Package

Main report sources are `analysis.qmd` and `analysis.Rmd`. Rendered outputs are `analysis.pdf` and `analysis.html`.

Cleaned data used in the report are in `data/derived/`. Report figures are in `output/figures/`, and supporting tables are in `output/tables/`.

Core scripts are in `scripts/01_cleaning_module.R` and `scripts/02_main_analysis.R`.

An anonymous repository link can be added later in the Code Appendix placeholder.
